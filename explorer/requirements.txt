streamlit>=1.30.0
streamlit-mermaid>=0.2.0
streamlit-agraph>=0.0.45
pyyaml>=6.0
python-frontmatter>=1.0.0
plotly>=5.18.0
pandas>=2.0.0
Pillow>=10.0.0
pygments>=2.17.0
whoosh>=2.7.4
matplotlib>=3.7.0
numpy>=1.24.0
# Segmentation Lab (classical algorithms + metrics, CPU-only)
scikit-image>=0.21.0
scipy>=1.10.0
# Playground: remote inference via the Hugging Face Inference API.
# Small, pure-Python, and enough to run every model in the Playground
# without torch — the default on memory-capped hosts.
huggingface-hub>=0.34.0
# Playground: local inference (optional — heavy).
# torchvision is NOT optional alongside transformers>=5: every SegFormer
# image processor (torchvision *and* PIL variants) declares
# `@requires(backends=("torch", "torchvision"))`, so without it
# AutoImageProcessor raises "Could not load any image processor class".
transformers>=4.30.0
torch>=2.0.0
torchvision>=0.15.0
